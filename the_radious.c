#include <stdio.h>

int main()
{
	int radius;  // the distance from outside to center of the cercle
	printf("pleas enter  radius:");

	scanf("%i",  &radius); // adress-of operator

	
	printf("the given radius is %i\n",radius );
	return 0;
}
